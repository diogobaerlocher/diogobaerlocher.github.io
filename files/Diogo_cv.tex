% Diogo Baerlocher -- CV
%
% The body of this document is GENERATED. To change content, edit either
%   _data/cv.yml                             (everything except papers)
%   _publications/ _projects/ _portuguese/   (papers; shared with the website)
% then regenerate:
%   python3 files/build_cv.py
%   latexmk -pdf -cd files/Diogo_cv.tex
%
% Only formatting lives in this file. Notes and retired content belong in
% _data/cv.yml under `include: false`, where they stay structured and greppable.

\documentclass[mm, 10pt]{simple_style}

% Default font is the helvetica postscript font
%\usepackage{helvet}

% Do not reduce `right` below 2in. simple_style.cls sets \hoffset=98.26pt
% (1.36in) to hang the section labels in the left margin, which shifts the whole
% text block right; `right=2in` compensates and leaves an effective 0.64in
% margin. right=1in overflows the page edge.
\usepackage[left=0.7in, right=2in, top=0.9in]{geometry}
\usepackage{setspace, longtable, graphicx, hyphenat, hyperref, fancyhdr, ifthen, everypage, enumitem, amsmath, setspace,amsfonts, palatino, amssymb}

% T1 encoding so accented coauthor names (Eustáquio, José, João, Econômica)
% typeset and hyphenate correctly.
\usepackage[T1]{fontenc}

% \needspace, used by the generator to stop a paper entry splitting across pages.
\usepackage{needspace}

% Scratch lengths for the contact table. The generator measures the actual
% e-mail and website cells into these, so the columns fit the real content.
\newlength{\cvcontactleft}
\newlength{\cvcontactright}
\newlength{\cvcontacttmp}

\usepackage[dvipsnames]{xcolor}
\hypersetup{colorlinks=true, linkcolor=RoyalBlue, urlcolor=Blue}
% Set font to Libertine, including math support
% \usepackage{libertine}
% \usepackage[libertine]{newtxmath}
\usepackage[sc,osf]{mathpazo}
% Increase text height
\textheight=700pt

\begin{document}

%-------------------------------------------------------------------------------
%	NAME AND VERSION SECTION
%-------------------------------------------------------------------------------
\name{Diogo Baerlocher}
\version{\today}

%-------------------------------------------------------------------------------

\begin{resume}

\input{cv_body}

\end{resume}
\end{document}
